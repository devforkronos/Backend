[build]
  command = "npm run build"
  publish="dist"
  base = "Client"
  # The base directory should be the path to the nested Vue project
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
